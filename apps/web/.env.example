# Server-side destination for the built-in same-origin proxy.
RECALLROOT_API_URL=http://127.0.0.1:8000

# Optional direct browser API URL. Leave unset to use /api/recallroot.
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Used by evidence links when the API does not return a per-span URL.
NEXT_PUBLIC_SIGNOZ_URL=http://localhost:3301
